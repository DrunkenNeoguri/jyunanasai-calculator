import localFont from "next/font/local";

export const HaneulBoriOFT = localFont({
  src: "./SKYBORI.otf",
  display: "swap",
  variable: "--font-mona-sans",
});

export const HaneulBoriTTF = localFont({
  src: "./SKYBORI.ttf",
  display: "swap",
  variable: "--font-mona-sans",
});
